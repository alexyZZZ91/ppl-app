# Copy to .env and fill in for authenticated Playwright tests.
# .env is gitignored — never commit real credentials.
# The account must be a DEDICATED test user in Supabase (auto-confirmed),
# fully onboarded once (handle + plan) so login lands straight in the app.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
